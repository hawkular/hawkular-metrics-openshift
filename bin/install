
touch "$OPENSHIFT_HOMEDIR"/.npmrc
mkdir "$OPENSHIFT_HOMEDIR"/.node-gyp
mkdir "$OPENSHIFT_HOMEDIR"/.npm
mkdir "$OPENSHIFT_HOMEDIR"/.bower
chown `id -u $OPENSHIFT_GEAR_UUID` -R "$OPENSHIFT_HOMEDIR"/.npmrc "$OPENSHIFT_HOMEDIR"/.node-gyp "$OPENSHIFT_HOMEDIR"/.npm "$OPENSHIFT_HOMEDIR"/.bower
